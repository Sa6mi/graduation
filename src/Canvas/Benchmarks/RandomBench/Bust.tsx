/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
*/

import * as THREE from 'three'
import React, { useRef, useMemo, useContext, createContext, PropsWithChildren } from 'react'
import { useGLTF, Merged } from '@react-three/drei'
import { GLTF } from 'three-stdlib'

type GLTFResult = GLTF & {
  nodes: {
    Mesh_0001: THREE.Mesh
  }
  materials: {
    ['default']: THREE.MeshStandardMaterial
  }
}

const context = createContext<any>(null)
export function Instances({ children, ...props }: PropsWithChildren<Record<string, any>>) {
  const { nodes } = useGLTF('/bust/bust-1-d.glb') as GLTFResult
  const instances = useMemo(
    () => ({
      Mesh: nodes.Mesh_0001,
    }),
    [nodes]
  )
  return (
    <Merged meshes={instances} {...props}>
      {(instances: unknown) => <context.Provider value={instances} children={children} />}
    </Merged>
  )
}

export function Model(props: JSX.IntrinsicElements['group']) {
  const instances = useContext(context)
  return (
    <group {...props} dispose={null}>
      <instances.Mesh position={[0, 0.386, 0]} />
    </group>
  )
}

useGLTF.preload('/bust/bust-1-d.glb')